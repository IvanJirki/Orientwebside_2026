services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_ADMIN_PASSWORD: ${VITE_ADMIN_PASSWORD:-}
    ports:
      - "${HOST_PORT:-8080}:80"
    restart: unless-stopped
